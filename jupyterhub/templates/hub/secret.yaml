kind: Secret
apiVersion: v1
metadata:
  name: hub-secret
type: Opaque
data:
  proxy.token: {{ default .Values.token.proxy .Values.proxy.secretToken | b64enc | quote }}
  hub.cookie-secret: {{ .Values.hub.cookieSecret | b64enc | quote }}
